<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Page de Test -Groupe LEVEL</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f5f5f5; margin: 0; }
        .navbar {
            background: #222;
            color: #fff;
            padding: 0.8rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        .navbar .brand {
            font-size: 1.3rem;
            font-weight: bold;
            letter-spacing: 1px;
        }
        .navbar nav a {
            color: #fff;
            text-decoration: none;
            margin-left: 1.5rem;
            font-size: 1rem;
            transition: color 0.2s;
        }
        .navbar nav a:hover {
            color: #4fc3f7;
        }
        .grid {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            justify-content: center;
            margin: 2rem;
        }
        .card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            width: 400px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }
        .card-title {
            padding: 1rem;
            font-size: 1.2rem;
            font-weight: bold;
            background: #0078d7;
            color: #fff;
        }
        .card-iframe {
            border: none;
            width: 100%;
            height: 300px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div style="display: flex; align-items: center; gap: 2rem;">
            <span class="brand">{{ ue }}</span>
            <span class="navbar-title" style="font-size:1.1rem; color:#4fc3f7; font-weight:500;">{{ groupe }}</span>
        </div>
        <nav>
            <a href="/">Accueil</a>
            <a href="/html">HTMLView</a>
            <a href="/json">JSONView</a>
            <a href="/book/1">TemplateView</a>
        </nav>
    </div>
    <div class="grid">
        {% for card in cards %}
            <div class="card">
                <div class="card-title">{{ card.title }}</div>
                <iframe class="card-iframe" src="{{ card.url }}" allowfullscreen></iframe>
            </div>
        {% endfor %}
    </div>
</body>
</html>